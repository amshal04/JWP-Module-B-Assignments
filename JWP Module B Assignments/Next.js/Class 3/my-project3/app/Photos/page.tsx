import React from 'react'

const Photos = () => {
  return (
    <div>
      <h1>Photo Page</h1>
      {
        getData ? getFlightDataPartsFromPath.map((e:PhotoSettings,i:number))
        return (
          <div>
          <h3>{else.title}</h3>
          </div>

        )

      })
    </div>
  )
}

export default Photos
